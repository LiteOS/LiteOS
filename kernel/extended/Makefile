include $(LITEOSTOPDIR)/config.mk

MODULE_$(LOSCFG_KERNEL_CPUP) += cpup
MODULE_$(LOSCFG_KERNEL_TICKLESS) += tickless
MODULE_$(LOSCFG_KERNEL_TRACE) += trace

MODULE_$(LOSCFG_KERNEL_CPPSUPPORT) += cppsupport

include $(MODULE)